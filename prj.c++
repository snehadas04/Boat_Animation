#include<graphics.h>

int boatColor;
int circleColor;
int flagColor;
void ship(int speed)
{
    setColor(boatColor);
    int s = speed;
    setFillStyle(SOLID_FILL,boatColor);
    for(int i=0;i<3;i++)
}